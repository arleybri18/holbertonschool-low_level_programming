#include "holberton.h"

/**
 * wildcmp - calculate sqrt using recursively i * i
 * @s1: value of a compare
 * @s2: value s2
 *
 * Return: Return value of i
 */

int wildcmp(char *s1, char *s2)
{
	int i;

	i = 0;
	return (is_prime_number2(n, i));
}
