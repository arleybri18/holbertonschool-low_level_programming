#include "holberton.h"

/**
 * is_palindrome - calculate sqrt using recursively i * i
 * @n: value of a compare
 *
 * Return: Return value of i
 */

int is_palindrome(int n)
{
	int i;

	i = 0;
	return (is_prime_number2(n, i));
}
