#include "holberton.h"

/**
 * mul - returns the product of two numbers
 * @a: First parameter
 * @b: The character to multiplicate a number
 *
 * Return: On success 0.
 */

int mul(int a, int b)
{
	a = a * b;
	return (a);
}
